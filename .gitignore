# This repo is PUBLIC. Private scratch — never committed (strategy, briefs, notes).
# A second copy of the preserved brief lives outside the repo at ~/dev/_private/wordmesh/.
_drafts/
_private/

# Secrets and key material
.env
.env.*
*.pem
*.key

# Evidence screenshots go to the PRIVATE vault (~/dev/_private/evidence), never here
evidence/
